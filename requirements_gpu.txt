# GPU-Accelerated AIInvigilator Requirements
# Install with: pip install -r requirements_gpu.txt

# Core Django and Web
Django==6.0.2
Pillow==11.1.0
mysqlclient==2.2.7
django-environ==0.12.0

# Computer Vision
opencv-python==4.11.0.86
numpy==2.2.1

# GPU-Accelerated PyTorch (CUDA 12.1)
# For CUDA 11.8, change cu121 to cu118
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# YOLO with GPU support
ultralytics==8.3.0

# Utilities
python-docx==1.1.2
python-dotenv==1.0.0

# Optional: For even faster inference (requires TensorRT installation)
# onnxruntime-gpu==1.16.0
# tensorrt==8.6.1
